---
title: "ILI dans le réseau sentinelle"
author: "Institut Pasteur de Madagascar"
date: "2016-05-03"
output: pdf_document
---




```
Error in `[.data.table`(data, , include, with = F): column(s) not found: deb_sem, code, abv, abz, ahh, ajb, atb, bel, bhk, boe, bos, cda, die, dri, ejd, far, fns, iho, mae, mdv, mhj, mia, mjr, mnj, mrb, mrg, mrt, mtn, nsb, sbv, stm, tdd, tgr, tlr, toa, tsl
```

```
Error in `[<-.data.table`(x, j = name, value = value): RHS of assignment to new column 'occurence' is zero length but not empty list(). For new columns the RHS must either be empty list() to create an empty list column, or, have length > 0; e.g. NA_integer_, 0L, etc.
```

```
Error in `[.data.table`(mydata, , `:=`(occurence, sum(occurence, na.rm = T)), : objet 'occurence' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'occurence' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'd' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'd' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'd' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'd' introuvable
```

```
Error in print(d): objet 'd' introuvable
```


```
Error in `[.data.table`(data, , include, with = F): column(s) not found: deb_sem, code, abv, abz, ahh, ajb, atb, bel, bhk, boe, bos, cda, die, dri, ejd, far, fns, iho, mae, mdv, mhj, mia, mjr, mnj, mrb, mrg, mrt, mtn, nsb, sbv, stm, tdd, tgr, tlr, toa, tsl
```

```
Error in eval(expr, envir, enclos): objet 'occurence' introuvable
```

```
Error in is.data.frame(x): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'occurence' introuvable
```

```
Error in `[<-.data.table`(x, j = name, value = value): RHS of assignment to new column 'occurence' is zero length but not empty list(). For new columns the RHS must either be empty list() to create an empty list column, or, have length > 0; e.g. NA_integer_, 0L, etc.
```

```
Error in eval(expr, envir, enclos): objet 'occurence' introuvable
```

```
Error in unique(X[, list(sites, vals)], by = NULL): objet 'X' introuvable
```

```
Error in merge(X, sentinel_latlong[, list(name, sites)], by.x = "sites", : objet 'X' introuvable
```

```
Error in eval(expr, envir, enclos): objet 'X' introuvable
```

```
Error in is.data.frame(x): objet 'X' introuvable
```

```
Error in inherits(x, "list"): objet 'X' introuvable
```


```
Error in merge.data.table(mydata, sentinel_latlong, by.x = "sites", by.y = "sites"): x has some duplicated column name(s): Long.x,Lat.x,name.x,centre.x,fokontany.x,Long.y,Lat.y,name.y,centre.y,fokontany.y. Please remove or rename the duplicate(s) and try again.
```

```
Error in eval(expr, envir, enclos): objet 'name' introuvable
```
